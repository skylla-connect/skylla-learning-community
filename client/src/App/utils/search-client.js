// import { withFirebase } from "../../firebase";

// const SearchClient = ({query}) = ({firebase}) => {
//         return firebase.doSearch(query);  
// }
// export default withFirebase(SearchClient);